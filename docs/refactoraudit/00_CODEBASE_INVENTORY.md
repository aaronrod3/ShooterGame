# 00 — Codebase Inventory

Full inventory of `Source/ShooterGame/` C++ classes plus the project-owned `Content/` DataAsset/Blueprint layer, produced for the pre-Wave-Mission refactor audit. Read alongside [01_REFACTOR_FINDINGS.md](01_REFACTOR_FINDINGS.md) for the analysis this table feeds.

## Note on source documents

The prompt that requested this audit named `MASTER_GUIDE.pdf`, `ALL_ISSUES.pdf`, `Gameplay-Plan.pdf`, and `GITHUB_SETUP_STEPS.pdf` as Step-0 reading. None of these files exist anywhere in this repository or working directory (confirmed via full-repo search). This audit substitutes the real, present equivalents instead of blocking on missing files:

| Requested | Substituted with |
|---|---|
| `MASTER_GUIDE.pdf` | [`CLAUDE.md`](../../CLAUDE.md) (repo root) — the actual architecture/layer/convention source of truth |
| `ALL_ISSUES.pdf` | Live GitHub Issues via `api.github.com/repos/aaronrod3/ShooterGame/issues` — **5 open issues total**, all epics (#1 Player Input, #2 Health/Damage/PlayerState, #3 Weapon System, #4 Mission State Machine and Phase Flow, #5 Zone and Wave Spawn Orchestration). No closed issues exist. Full bodies of #4 and #5 (the two most relevant to this plan) were read in full and are cited throughout. |
| `Gameplay-Plan.pdf` | [`docs/GameplayPlan.md`](../GameplayPlan.md) |
| `docs/WaveMission_DemoPlan/` | Read as-is — this is the real, already-generated 2-month plan this audit cross-references |
| `GITHUB_SETUP_STEPS.pdf` | `.github/ISSUE_TEMPLATE/*.yml` (bug/feature/playtest/tech-task templates), live label list (36 labels), live milestone list (6 milestones, `Pre-Production Refresh` through `Release Candidate`) — all read directly from the repo/API |
| `PROGRESS.md` at repo root | Read in full — a prior session's Rider "Inspect Code" cleanup log; informs the code-quality baseline (const-correctness, naming, unused-include debt already partially addressed) |

Also read: `Prompt Summaries/Systematic_Audit_2026-07-07.md` and `Prompt Summaries/Phase3Issues.md` — prior investigation notes on the FP/TP camera system. One finding they flagged (both cameras/meshes visible simultaneously at spawn) was **verified fixed** in the current, uncommitted `ShooterGameCharacter.cpp` (see `ApplyViewState()` call added at the end of `BeginPlay()`) — not re-reported here, since it no longer reproduces against current source.

## Content/ directory structure (names only — per CLAUDE.md, `.uasset` files are never parsed as raw binary)

```
Content/AI/            — zombie BT/BB/AIController Blueprints
Content/Animation/      — AnimBP, blend spaces, montages (Infima + project-owned)
Content/Animations/     — (second, separate animation folder — see 01_REFACTOR_FINDINGS.md Nice-to-have)
Content/Assets/
Content/Character/      — BP_PlayerCharacter, ABP_FP_Default, ABP_TP_Default, Input/
Content/Collections/
Content/Core/
Content/Data/            — DataAssets: DA_556 (ammo), DA_EquipmentSlotConfig
Content/Debug/
Content/Developers/
Content/Effects/
Content/Enemy/
Content/Framework/
Content/HUD/
Content/Inventory/
Content/Items/           — DA_RifleBaseConfig, DA_AssaultRifle, DA_Rifle_Config, DA_556_Magazine
Content/LevelPrototyping/
Content/ThirdPerson/
```

No `Zone`, `Mission`, `Wave`, `Economy`, `Resupply`, or `Extraction`-named content exists anywhere in `Content/` — confirms these are green-field for Wave Mission Mode, consistent with `Source/` having no matching classes either (see Section "Mission/Economy — currently empty" below).

---

## Framework/ — GameMode, PlayerState, GameInstance, Subsystems

| File | Purpose | Layer | Size | Hardcoded Values? | Uses EIS? | Replication OK? | Code Smells |
|---|---|---|---|---|---|---|---|
| `Framework/GameMode/ShooterGameGameMode.h/.cpp` | Match lifecycle: alive-player tracking, downed/died/revived hooks, friendly fire toggle, restart flow | Foundation (cross-cutting) | 104h / 266cpp | No — `PostMatchDelay` is EditAnywhere | N/A | ✅ Extends `AGameModeBase` with **no paired `AGameStateBase`** — this is the load-bearing gap, see Blocking finding | Uses `LogTemp` throughout (no custom log category, project-wide); `AlivePlayers` has no "extracted" status — see Blocking finding |
| `Framework/PlayerState/ShooterPlayerState.h/.cpp` | Replicated loadout/appearance owner, save-subsystem bridge, seamless-travel `CopyProperties` | Foundation | 77h / 188cpp | No | N/A | ✅ Clean `Server_X`/`OnRep_X`/delegate pattern, exactly per CLAUDE.md convention | Two independently-replicated loadout copies (`SavedLoadout` here vs. `LoadoutComponent::LoadoutData`) kept in sync only by explicit push calls — see Blocking finding (Kit Presets) |
| `Framework/GameInstance/ShooterGameGameInstance.h/.cpp` | Thin GameInstance subclass | Foundation | 41h / 45cpp | No | N/A | N/A | None found — trivial file |
| `Framework/Subsystems/ShooterSaveGameSubsystem.h/.cpp` | Sole disk I/O point (per CLAUDE.md); loadout/appearance/equipped-state/loadout-preset/quest-state save+load | Foundation | 172h / 535cpp | No | N/A | N/A (local subsystem) | Comprehensive and well-scoped; already has `SaveLoadoutPresets`/`LoadLoadoutPresets` and `BuildEquippedStateSnapshot`/`ApplyEquippedStateSnapshot` — directly relevant, see Blocking finding (Kit Presets) |
| `Framework/Subsystems/ShooterSaveGame.h/.cpp` | `USaveGame` data container | Foundation | 139h / 30cpp | N/A | N/A | N/A | Not independently reviewed line-by-line; referenced correctly by SaveGameSubsystem |
| `Framework/Subsystems/QuestTrackerSubsystem.h/.cpp` | Per-session questbook + vendor reputation tracker | Layer 2 (existing, pre-dates Wave Mission scope) | 219h / 370cpp | No | N/A | ⚠ **`UGameInstanceSubsystem` with flat, non-player-keyed arrays; class's own header comment admits mutations aren't yet server-RPC-routed for "a fully networked session"** | Anti-pattern reference — see Blocking finding (Economy); do not copy this subsystem's authority model for `UScoringSubsystem` |
| `Framework/Subsystems/ShooterGameOnlineSubsystem.h/.cpp` | Online Subsystem wrapper | Foundation | 137h / 270cpp | Not deeply reviewed | N/A | N/A | Not in scope — no Wave Mission dependency found |
| `Framework/ShooterGame.h/.cpp` | Module boilerplate | N/A | 7h / 7cpp | N/A | N/A | N/A | Trivial |

## Player/ — Character, Controller, Animation, Spectator

| File | Purpose | Layer | Size | Hardcoded Values? | Uses EIS? | Replication OK? | Code Smells |
|---|---|---|---|---|---|---|---|
| `Player/Character/ShooterGameCharacter.h/.cpp` | Player pawn — composes Combat/Downed/Revive/Loadout/EquippedState/Inventory components, **plus** owns camera, prone, sprint/stamina, health, interaction-trace, and IK config directly | Foundation | 580h / **1662cpp** | Mostly EditAnywhere tunables (correct per convention) — 12 camera fields, 5 sprint fields, IK socket names as `FName` defaults | ✅ Yes (only file besides Controller/SpectatorPawn using `UInputAction`/Enhanced Input) | ✅ `GetLifetimeReplicatedProps` present, `Server_`/`OnRep_` pattern followed correctly | **God-class tendency**: despite CLAUDE.md describing this as a "thin shell composing single-responsibility components," camera/shoulder-swap/prone/sprint/health/interaction-trace/view-toggle logic (~300+ lines across 5-6 functions) all live directly on the character, not in components — see High-value finding. Duplicated `Health`/`MaxHealth`/`OnRep_Health` pattern vs. `AZombieCharacter` (no shared `UHealthComponent`) — see High-value finding |
| `Player/Controller/ShooterGamePlayerController.h/.cpp` | Input-driven interactable tracing, HUD widget mgmt, spectator possession, match-over flow | Foundation | 114h / 295cpp | `TraceLength=500.f`, `ItemTraceChannel` hardcoded in constructor rather than `EditDefaultsOnly` | ✅ | N/A (local-only) | Runs `TraceForItem` every `Tick` with no early-out; `FindComponentByInterface` called every frame rather than cached; several silent-fail casts. Confirmed: interaction call pattern (`Execute_CanInteract`/`Execute_GetInteractPromptText`/`Execute_Interact`) is **fully generic** — new Interactable classes need zero controller changes |
| `Player/Spectator/ShooterGameSpectatorPawn.h/.cpp` | Spectates alive players, cycles targets | Foundation | 90h / 151cpp | `BlendTime=0.5f` hardcoded in `.cpp` rather than UPROPERTY | N/A | `bReplicates=false` — correct, local-only | Modulo arithmetic on `AliveTargets.Num()` unguarded against zero; no null-check before `GetName()` on next-target cycle |
| `Player/Animation/ShooterAnimInstanceBase.h/.cpp` | Shared FP/TP anim instance base — the "Phase 1 Infima bridge" contract | Foundation | 276h / 278cpp | Several: `IdleSpeedThreshold=10.f`, `RunSpeedThreshold=150.f`, `SprintSpeedThreshold=250.f`, `GripInterpSpeed=10.f` (all EditAnywhere, fine); hardcoded socket/bone name literals `"SOCKET_Grip"`, `"SOCKET_Grip_R"`, `"ik_hand_l"`, `"ik_hand_r"` embedded in `.cpp` rather than config | N/A | N/A (local anim instance) | Confirmed bridge properties: `bIsWalking`/`bIsRunning`/`bIsSprinting`, `InputMoveVector`, `CrouchTransform`/`AimDownSightsTransform`/`RecoilTransform`, `CurrentGripAlpha`/`GripInterpSpeed`, `LeftHandTransform`/`bLeftHandOnWeapon`/`RightHandTransform`. Flat ~40-property bag, no grouping structs — Nice-to-have only, no Wave Mission dependency |
| `Player/Animation/PlayerAnimInstance.h/.cpp` | Legacy animation instance — appears to duplicate `ShooterAnimInstanceBase` | Foundation | 86h / 110cpp | `45.f` lean clamp hardcoded in `.cpp` | N/A | N/A | ⚠ **NEEDS RUNTIME VERIFICATION** — near-identical property/method names to `ShooterAnimInstanceBase` (IK calc, lean, locomotion); unclear if any live AnimBP still targets this class or if it's dead code. Static reading cannot confirm which Blueprints reference it — check in-editor (Reference Viewer on this class) before deleting |
| `Player/Animation/ShooterFPAnimInstance.h/.cpp`, `ShooterTPAnimInstance.h/.cpp` | FP/TP specializations of the base | Foundation | 49h/22cpp, 24h/18cpp | No | N/A | N/A | TP variant is an empty stub (documented, intentional per Phase 4 of infima_integration_plan.md) |
| `Player/Animation/ShooterAnimStateInterface.h/.cpp` | Interface: `UpdateLeftHandGrip`, `SetAimingBlocked`, `OnAnimStateMessage` | Foundation | 54h / 5cpp | No | N/A | N/A | `OnAnimStateMessage(FName, float)` is an untyped catch-all with no documented valid tag set |
| `Player/Animation/ANS_BlockADS.h/.cpp`, `ANS_LeftHandGrip.h/.cpp` | Anim notify-states (both marked **[DONE]** in infima_integration_plan.md) | Foundation | 33h/38cpp, 42h/46cpp | `BlendOffSpeed=10.f`/`BlendOnSpeed=8.f` (EditAnywhere, fine) | N/A | N/A | `ANS_LeftHandGrip`: if the `UShooterAnimInstanceBase*` cast fails in `NotifyEnd`, the grip override is never cleared (dangling state) |

## Components/ — Gameplay Components

| File | Purpose | Layer | Size | Hardcoded Values? | Uses EIS? | Replication OK? | Code Smells |
|---|---|---|---|---|---|---|---|
| `Components/CombatComponent.h/.cpp` | Fire/reload/ADS/recoil/hip-fire-turn/suppressor/combat-action state machine | Foundation | 380h / **1370cpp** | No — all tunables are `EditAnywhere` (correct); two hardcoded socket-name fallbacks (`"RightHandSocket"`, `"MuzzleFlash"`) and one hardcoded `TraceDistance=50000.f` embedded in `.cpp` rather than sourced from `WeaponConfig` | N/A | ✅ 5 replicated properties, 4 `OnRep_`, 6 `Server_` RPCs, 3 `NetMulticast` — pattern followed correctly | **God-class**: 16+ manually-managed state flags (`bFullAutoFiring`, `bBurstInProgress`, `bFiredThisPress`, `bPendingHipFireShot`, `bLocalReloadPending`, `bInCombatState`, ...); `HandleFire` (~116 lines) duplicates `ApplyDownedDebuffsPreFire`/`ShouldDelayHipFireShot` checks across 3 fire-mode branches; `friend class AShooterGameCharacter/AShooterGamePlayerController` (tight coupling via friendship). **`SpawnAndEquipWeaponFromSlot` only equips the first occupied loadout slot found (early-breaks), does not loop all slots** — see Blocking finding (Kit Presets). No public "combat intensity" signal for external systems to read — High-value finding (not currently required by the WaveMission_DemoPlan as designed, since that plan drives pressure from zone/task state, not combat activity) |
| `Components/DownedComponent.h/.cpp` | Downed state, bleedout timer, activity-based drain rate, hit-spike | Foundation | 182h / 381cpp | Bleedout log thresholds `{50,40,30,20,10,5,4,3,2,1}` hardcoded as literal array in `.cpp` rather than config | N/A | ✅ 2 replicated props, `OnRep_` pair, 3 `Server_` RPCs | `// TODO: Medical kit stub` (line ~199) — deliberate, matches project convention of declared stubs |
| `Components/ReviveComponent.h/.cpp` | Proximity revive, hold-to-revive, interrupt handling | Foundation | 127h / 246cpp | No | N/A | Partial — no `GetLifetimeReplicatedProps` override (state is client-local/server-driven via `DownedComponent`, which is a deliberate design, not a gap) | `// TODO: Team check goes here when team system is implemented` (line ~113) — corroborates the missing-team-system finding, see Blocking finding (Temporary Squad Member / Human AI) |
| `Components/LoadoutComponent.h/.cpp` | Replicated `FLoadoutData`/`FCharacterAppearance` owner, slot validation | Foundation | 168h / 158cpp | No | N/A | ✅ Clean — `Server_SetSlot`/`Server_SetFullLoadout`/`OnRep_LoadoutData`→`OnLoadoutChanged` delegate, exactly per CLAUDE.md pattern | `CanEquipInSlot` synchronously loads the item class CDO (acceptable at lobby time only, per its own comment) — Nice-to-have if ever called mid-mission |
| `Components/HitZoneComponent.h/.cpp` | Bone→hit-zone→damage-multiplier lookup, shared by player + zombie | Foundation | 63h / 95cpp | **Yes — bone-name-to-zone map and all damage multipliers (Head 4.0, Neck 2.5, Torso 1.0, Arm 0.75, ...) are hardcoded in the C++ constructor, not `EditAnywhere`/DataAsset-driven** | N/A | `SetIsReplicatedByDefault(false)` — correct, server-only per CLAUDE.md | Config-in-constructor instead of DataAsset — Nice-to-have (no Wave Mission system requires retuning this) |
| `Components/AudioPerceptionComponent.h/.cpp` | Generic "this actor made noise" emitter — `Server_EmitSoundEvent`/`ReportNoiseEvent` | Foundation | 108h / 156cpp | One: occlusion check height `FVector(0,0,200.f)` hardcoded in `.cpp` | N/A | ✅ `SetIsReplicatedByDefault(true)`, `Server_EmitSoundEvent` with validation | **Already the exact mechanism File 02/05 of the demo plan need for "ammo crate attracts enemies" / "distraction flare" — no new AI Perception wiring is required, just call this component's existing API.** See positive cross-reference correction in 01_REFACTOR_FINDINGS.md |
| `Components/ImpactAudioComponent.h/.cpp`, `ShellAudioComponent.h/.cpp` | Surface-type impact audio (bullet impacts / shell casings) | Foundation | 55h/81cpp, 64h/38cpp | Surface-type-to-cue mapping hardcoded per component | N/A | Impact: ✅ Server+Multicast RPC. Shell: local-only, correct | **Near-identical `GetCueForSurface`/`GetImpactCueForSurface` logic duplicated across both files** — Nice-to-have merge candidate, zero Wave Mission dependency |
| `Components/WeaponAudioComponent.h/.cpp` | Full weapon audio (one-shot/loop/tail × suppressed/unsuppressed × dry-fire/reload/mode-switch) | Foundation | 294h / 520cpp | Timing offsets (`0.04f`, `0.05f`, `0.2f`) all `EditAnywhere`, fine | N/A | RPC-routed via owning weapon actor | 12 `Server_`/`Multicast` RPC pairs, ~24 parallel cue/audio-component arrays — large but cohesive to its single responsibility (weapon audio); Nice-to-have only |
| `Components/ShooterSoundFunctionLibrary.h/.cpp` | Thin wrapper over `UGameplayStatics` sound spawning | Foundation | 99h / 93cpp | No | N/A | N/A | Minimal added value over calling `UGameplayStatics` directly — Nice-to-have |

## Enemy/ — Zombie AI

| File | Purpose | Layer | Size | Hardcoded Values? | Uses EIS? | Replication OK? | Code Smells |
|---|---|---|---|---|---|---|---|
| `Enemy/ZombieSpawnManager.h/.cpp` | Designer-placed actor: `TriggerSpawn`/`FireObjectiveSpawnEvent`/`ResetForNextWave`/`GetAliveZombieCount`, Single/Group/Random modes, nav-mesh-reachable spawn point resolution | Layer 1 (existing) — **direct ancestor of the Layer-3 Wave Spawn system** | 128h / 481cpp | `SpawnLocation.Z += 50.f`, retry delay `0.1f`, debug values — minor, low-impact | N/A | `bReplicates=false` — correct, server-only orchestrator (spawned zombies replicate themselves) | Uses `LogTemp` throughout. **This class already implements one-shot wave-gating (`bObjectiveEventFired`) and a `ResetForNextWave()` API that is conceptually a proto-wave-system** — see Blocking finding (Phase Triggers / Wave Spawning), directly relevant to GitHub Issue #5 |
| `Enemy/ZombieObjectiveArea.h/.cpp` | Placed area marker zombies are routed to; `GetRandomPointInRadius`, `RegisterRoutedZombies` | Layer 1 (existing) — proto-zone marker | 66h / 160cpp | `ArrivalRadius=400.f` (EditAnywhere, fine) | N/A | No replication — correct, static server-only map marker | **This is structurally a simplified precursor to `AZoneDefinition`** (has a radius and a name, lacks a state enum or per-phase spawn tables) — see Blocking finding (Phase Triggers) |
| `Enemy/Character/ZombieAIController.h/.cpp` | BT/Blackboard driver: perception (sight+hearing already configured), group-alert broadcast, investigate/chase/attack state | Layer 1 (existing) | 114h / 749cpp | `FGenericTeamId(1)` hardcoded team literal (line 26) | N/A | N/A (AI controllers aren't independently replicated; zombie pawn state is) | **Hardcoded team ID with no attitude solver** — see Blocking finding (Human AI Reinforcement, Temporary Squad Member). `UAIPerceptionComponent` with Sight+Hearing **already configured** — File 02 of the demo plan can skip "add perception hearing sense" as a task, it exists |
| `Enemy/Character/ZombieCharacter.h/.cpp` | Replicated health, `EZombieState`, hit-zone-routed `TakeDamage`, crawler transition, melee | Layer 1 (existing) | 167h / 524cpp | No (all `EditAnywhere`) | N/A | ✅ `OnRep_Health`, `OnRep_ZombieState`, `Replicated bCanSprint`, `NetMulticast Multicast_PlayMeleeAttackMontage` | Duplicates `Health`/`OnRep_Health` pattern independently from `AShooterGameCharacter` — see High-value finding (extract shared `UHealthComponent`) |
| `Enemy/Character/ZombieAnimInstance.h/.cpp` | Zombie anim instance | Layer 1 | 53h / 67cpp | Not deeply reviewed | N/A | N/A | Not in scope |
| `Enemy/BehaviorTree/BTTask_InvestigateWander.h/.cpp`, `BTTask_WanderToPoint.h/.cpp` | Custom BT tasks | Layer 1 | 36h/146cpp, 32h/266cpp | Not deeply reviewed | N/A | N/A | Already cleaned up per `PROGRESS.md` categories 4/5/17/20 (dead locals, visibility, const-correctness) |

## Inventory/, Items/ — Item, Loadout, Weapon systems

| File | Purpose | Layer | Size | Hardcoded Values? | Uses EIS? | Replication OK? | Code Smells |
|---|---|---|---|---|---|---|---|
| `Inventory/InventoryComponent.h/.cpp` | Tag-gated `FItemInstance` container, server-authoritative add/remove/transfer/update | Foundation | 218h / 257cpp | `MaxItems=30`, `MaxMagazineSlots=6` defaults (EditDefaultsOnly, fine) | N/A | ✅ `Items`/`MagazineSlots` both `ReplicatedUsing`, full `Server_` API | Clean, well-structured — `GetItems()`/`Server_AddItem`/`Server_RemoveItem` give Kit Presets exactly what it needs for `FItemInstance` snapshot/restore. **The "magazine shim" (`AddMagazine`/`FMagazine`/`MagazineSlots`) is a second, parallel item-storage system CLAUDE.md already flags as "being phased out — do not extend it"** — see Blocking finding (Kit Presets) |
| `Inventory/EquippedStateComponent.h/.cpp` | 7 named equipment slots (primary/secondary/sidearm/helmet/chest/tool/knife) + rig/belt/backpack containers, weight/burden, `Server_LootDeadPlayer` | Foundation | 125h / 350cpp | `DeadPlayerBurdenMultiplier=1.0f` (EditAnywhere, fine) | N/A | ✅ 7× `ReplicatedUsing` slots + weight fields, 3 `Server_` functions | Repetitive `OnRep_*` stubs across 7 slots (could share one generic path); **`Server_LootDeadPlayer` is an existing lootable-corpse mechanic that File 04's "full gear loss" design must explicitly reconcile with** — see Blocking finding (Extraction) |
| `Inventory/ItemDefinition.h/.cpp` | `UPrimaryDataAsset` — identity, slots, magazine limits, weight/stack, quest flag, vendor pricing, currency (`bIsCurrency`/`CurrencyValue`), durability | Foundation | 287h / 10cpp | Field defaults only (all `EditDefaultsOnly`, correct) | N/A | N/A (DataAsset) | Pure property bag, no validation methods (`IsStackable()`, etc. don't exist) — Nice-to-have. **Already has a full physical-currency-item concept (`bIsCurrency`/`CurrencyValue`/vendor pricing) — distinct from Wave Mission's proposed abstract `PersonalPoints`/`SquadSharedFund`.** See High-value clarification (Economy) |
| `Items/Weapon/WeaponConfig.h/.cpp` | `UPrimaryDataAsset` mirroring Infima's `BP_TFA_BaseConfig` — meshes, sockets, montages (FP/TP), sounds, offsets | Foundation | 658h / 8cpp | Field defaults only, correct | N/A | N/A (DataAsset) | **Assessed as an excellent template to copy for `UWaveSetDataAsset`/`UCallInDefinition`** (TSoftObjectPtr refs, EditDefaultsOnly, extensive comments) — but has 3 parallel legacy-vs-new naming schemes for the same montage concepts and no nested sub-structs at 658 lines. New DataAssets should copy the good parts, not the naming drift — see High-value finding |
| `Items/Weapon/Weapon.h/.cpp` | Weapon actor — fire, ammo/feed, attach, state replication | Foundation | 443h / 512cpp | `CollisionSphere=96.f`, `"CaseEject"` socket name hardcoded in `.cpp` rather than sourced from `WeaponConfig` | N/A | ✅ 4 replicated props (`WeaponState`, `bRoundChambered`, `ReplicatedMagState`, `bHasSuppressor`), 3 `OnRep_` | Tight coupling to `WeaponAudioComponent`/`AudioPerceptionComponent` (direct calls, no interface seam); **dual ammo-state tracking (`InsertedMagazine` `TOptional` local + `ReplicatedMagState` replicated) manually kept in sync — desync risk under packet reorder** — Nice-to-have (no current Wave Mission dependency, but flag for File 08's networking hardening pass) |
| `Items/Weapon/ProjectileWeapon.h/.cpp`, `Projectile.h/.cpp` | Projectile-firing weapon variant, projectile actor | Foundation | 29h/64cpp, 75h/197cpp | `FallbackDamage=20.f` (fine) | N/A | ⚠ **Projectile actor itself is not replicated** — server-only simulation, no client-side visibility of projectile travel | Not a Wave Mission blocker as designed (hitscan is the primary path); flag only if a future weapon needs client-visible projectile travel |
| `Items/Weapon/CaseEject.h/.cpp` | Shell casing physics actor | Foundation | 110h / 150cpp | 10 physics tuning fields, all `EditAnywhere` (fine) | N/A | Not replicated — correct, cosmetic-only | Two parallel lifetime systems (hard cap + rest-detection) — Nice-to-have simplification, zero Wave Mission dependency |
| `Items/Ammo/AmmoData.h/.cpp`, `WeaponFeedTypes.h`, `AmmoPickup.h/.cpp` | Per-caliber damage DataAsset, `FMagazine` struct, world ammo pickup | Foundation | see inventory | Defaults only | N/A | `FMagazine` is replicated as part of `Weapon::ReplicatedMagState` | 2-zone damage model (head/body only) — Nice-to-have; `FMagazine::operator==` compares value not identity (no unique magazine tracking) — Nice-to-have |
| `Items/Components/ItemComponent.h/.cpp` | Generic pickup-message holder | Foundation | 34h / 26cpp | `"F - Pickup"` default string | N/A | Not replicated | Placeholder — `PickupMessage` stored but never consumed by any system found. Nice-to-have (remove or wire up) |

## HUD/, Interaction/, Quest/ — UI and world interaction

| File | Purpose | Layer | Size | Hardcoded Values? | Uses EIS? | Replication OK? | Code Smells |
|---|---|---|---|---|---|---|---|
| `Interaction/Interactable.h/.cpp` | Interface: `Interact`/`CanInteract`/`GetInteractPromptText` | Foundation | 33h / 1cpp | No | N/A | N/A (interface) | **Confirmed clean and generic** — call pattern in `ShooterGameCharacter.cpp`/`ShooterGamePlayerController.cpp` needs zero changes to support new implementers (`AObjectiveTerminal`, `AFieldExtractionPoint`) |
| `Interaction/QuestGiverInterface.h/.cpp` | Interface for quest-giving NPCs | Layer 2 (existing) | 69h / 1cpp | No | N/A | N/A | Well-modularized; no Wave Mission dependency |
| `Interaction/Highlightable.h/.cpp`, `HighlightableStaticMesh.h/.cpp` | Outline-highlight interface + static mesh implementation | Foundation | 44h/7cpp, 28h/16cpp | No | N/A | N/A | Stale placeholder comment block in `Highlightable.h`; Nice-to-have |
| `Interaction/TestInteractableActor.h/.cpp` | Test/reference `IInteractable` implementation | Foundation | 47h / 70cpp | `PromptText`/`bCanCurrentlyInteract` defaults hardcoded rather than `EditAnywhere` | N/A | ⚠ `bReplicates=true` set but **no `GetLifetimeReplicatedProps`, no `OnRep_`, no replicated state at all** — interaction result is not synchronized to clients | **Do not copy this actor's (lack of) replication pattern when building `AObjectiveTerminal`/`AFieldExtractionPoint`** — flagged explicitly in 01_REFACTOR_FINDINGS.md |
| `Quest/QuestDefinition.h/.cpp` | `UPrimaryDataAsset` — quest identity, objectives, rewards | Layer 2 (existing) | 132h / 62cpp | Defaults only | N/A | N/A (DataAsset) | Clean design-time/runtime separation (mirrors what File 03/05 of the demo plan already independently proposed) — no changes needed |
| `HUD/ShooterHUD.h/.cpp` | Crosshair reticle draw, HUD widget lifecycle | Foundation | 62h / 117cpp | Screen-center calc inline rather than helper | N/A | N/A (local) | Weak `BoundWeapon` pointer not null-checked before unbind; correct delegate-driven pattern otherwise |
| `HUD/InteractPromptWidget.h/.cpp`, `Widgets/HUDWidget.h/.cpp`, `EndScreen/ShooterEndScreenWidget.h/.cpp` | Interaction prompt, persistent HUD Blueprint parent, end-screen | Foundation | small | Minimal | N/A | N/A | Thin/stub — no smells beyond missing input validation |
| `HUD/Inventory/QuestbookWidget.h/.cpp`, `QuestObjectiveRowWidget.h/.cpp` | Quest UI, delegate-bound (not polling) | Layer 2 (existing) | 123h/117cpp, 52h/9cpp | `CachedObjectiveIndex` default | N/A | N/A | `NativeDestruct` doesn't null-check subsystem before unbinding; correct push-model via subsystem delegates otherwise |

## Types/ — Structs and Enums

| File | Purpose | Layer | Size | Notes |
|---|---|---|---|---|
| `Types/LoadoutTypes.h` | `EEquipmentSlot`, `EWeaponCategory`, `EItemCategory`, `FLoadoutSlot`, `FLoadoutData`, `FCharacterAppearance` | Foundation | 283 | Clean, well-documented, explicit save-compatibility rules already in header comments |
| `Types/ContainerTypes.h/.cpp` | `EEquipmentContainerType`, `FEquipmentContainerConfig`, `FNamedEquippedItem`, `FEquippedContainerState`, `FEquippedStateSnapshot`, **`FLoadoutPresetSlot`, `FLoadoutPreset`** | Foundation | 288h / 94cpp | **`FLoadoutPreset` is an already-built, already-save-wired kit-preset system referencing items by InstanceID into a persistent stash — structurally different from what `docs/WaveMission_DemoPlan/06_Kit_Presets_and_Loadout.md` proposes building from scratch.** Central Blocking finding, see 01_REFACTOR_FINDINGS.md |
| `Types/ItemTypes.h/.cpp` | `FItemInstance` and related runtime item structs | Foundation | 280h / 10cpp | GUID-identity convention followed correctly throughout |
| `Types/CombatTypes.h` | `ECombatAction`, `EReloadType`, `EWeaponGrip` | Foundation | 52 | Clean |
| `Types/HitZoneTypes.h` | `EHitZone` and related | Foundation | 141 | Not deeply reviewed |
| `Types/ZombieTypes.h` | `ESpawnMode`, `EZombieState`, `FZombieConfig` | Layer 1 (existing) | 196 | Extensive, well-tuned config struct — good template for `UWaveSetDataAsset`-adjacent per-enemy config |
| `Types/QuestTypes.h/.cpp`, `VendorTypes.h` | Quest/vendor runtime types | Layer 2 (existing) | 186h/26cpp, 59 | Not deeply reviewed |
| `Types/ShooterGameplayTags.h/.cpp` | `Slot.*` native GameplayTag declarations | Foundation | 37h / 17cpp | Only 8 tags exist (`Slot.Backpack/Rig/Belt/WeaponMod/Holster/MedPouch/MissionCache/Stash`) — no `Zone.*`, `Class.*`, `CallIn.*`, or `SquadCommand.*` families yet, confirming these are all additions the demo plan correctly scoped as new |
| `Types/AmmoType.h`, `FireMode.h`, `PlayerCombatState.h`, `PlayerWeaponStance.h`, `TurningInPlace.h`, `ItemContextMenuTypes.h`, `QuickSlotTypes.h` | Assorted small enums/structs | Foundation | small | Not individually reviewed — none flagged in either sub-agent pass |

## Mission/Economy — currently empty

No `Source/ShooterGame/Mission/`, `Economy/`, or equivalent folder exists. `grep -ri "Phase\|Wave\|MissionState\|EMissionPhase"` across all of `Source/` returns no mission-state-machine hits (only unrelated matches: audio "waveform," module boilerplate). `grep -ri "Points\|Currency\|Score\|SharedFund"` returns only the existing quest-reputation and item-currency hits catalogued above — **no points/economy system of any kind currently exists**, confirming `docs/WaveMission_DemoPlan/05_Economy_and_Resupply.md`'s "built from zero" assumption is correct.

---

## Cross-cutting observations feeding the findings file

1. **No custom log category anywhere in the project** — `grep -r "DEFINE_LOG_CATEGORY"` returns zero project-defined categories; every file uses `UE_LOG(LogTemp, ...)`. Given the tuning-heavy nature of the 8-week Wave Mission build (escalation curves, spawn rates, economy values — all explicitly meant to be iterated via live logging per the demo plan's risk mitigations), this is a Nice-to-have worth doing early.
2. **`class SHOOTERGAME_API` appears in 54 files** — the module boundary is consistently applied; no export/visibility issues found.
3. **Enhanced Input is used exclusively** — `grep` for `BindAction(`/`EKeys::`/legacy input finds nothing; only `UInputAction`/Enhanced Input across `ShooterGameCharacter.cpp`, `ShooterGameSpectatorPawn.cpp`, `ShooterGamePlayerController.cpp`. No dimension of this audit needs an EIS migration — it's already done project-wide.
4. **Replication is applied consistently** where it's needed (18 files implement `GetLifetimeReplicatedProps`) — the project's stated replication convention (CLAUDE.md) is followed in every file this audit inspected, with the one significant exception of `UQuestTrackerSubsystem` (flagged above) and the minor exception of `TestInteractableActor` (a test/reference actor, not shipped content).
